{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for FluxionJS Editor",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "opener:allow-open-url"
  ]
}
