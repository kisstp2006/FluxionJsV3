-- $CLS
-- Loads the scene assigned in the Inspector when the configured key is pressed.

nextScene = SceneRef()
loadKey   = "Space"

function update(dt)
    if not nextScene.isValid then return end
    if self.Input.isKeyPressed(loadKey) then
        self.Scene.load(nextScene.path)
    end
end
